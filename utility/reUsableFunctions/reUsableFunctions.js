export const isAdmin = role => role === 'admin';
export const isBodyNull = body => body === null;
export const isUserNull = user => user === null;
export const isParamsNull = params => params === null || params === undefined || params.length === 0 || params === "";
